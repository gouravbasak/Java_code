package OOPs;

public interface Shape_interface {
    public interface Shape {
        void draw();
        void printArea();
        //in interface, we don't need to mention public or abstract
        }
}
